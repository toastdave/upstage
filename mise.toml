min_version = "2025.1.0"

[tools]
bun = "1.3.10"

[tasks.install]
run = "bun install"
description = "Install workspace dependencies"

[tasks.dev]
depends = ["install"]
run = "bun run dev"
description = "Start the SvelteKit app locally"

[tasks."dev:docker"]
run = "docker compose up --build web"
description = "Start the fully containerized app"

[tasks."docker:up"]
run = "docker compose up -d postgres minio createbuckets mailpit"
description = "Start local infrastructure containers"

[tasks."docker:down"]
run = "docker compose down"
description = "Stop Docker services"

[tasks.check]
depends = ["install"]
run = "bun run check"
description = "Run type checks across the workspace"

[tasks.lint]
depends = ["install"]
run = "bun run lint"
description = "Run Biome linting"

[tasks.format]
depends = ["install"]
run = "bun run format"
description = "Format the repository with Biome"

[tasks.test]
depends = ["install"]
run = "bun run test"
description = "Run Bun tests across the workspace"

[tasks.build]
depends = ["install"]
run = "bun run build"
description = "Build the SvelteKit app"

[tasks."db:generate"]
depends = ["install"]
run = "bun run db:generate"
description = "Generate Drizzle SQL migrations"

[tasks."db:migrate"]
depends = ["install"]
run = "bun run db:migrate"
description = "Apply Drizzle migrations"

[tasks."db:push"]
depends = ["install"]
run = "bun run db:push"
description = "Push the schema directly to Postgres"

[tasks."db:studio"]
depends = ["install"]
run = "bun run db:studio"
description = "Open Drizzle Studio"

[tasks.seed]
depends = ["install"]
run = "bun run seed"
description = "Seed development data"
